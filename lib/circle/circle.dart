class Circle {
  final String id;
  final String name;
  final int x;
  final int y;
  final int color;
  const Circle(this.id, this.x, this.y, this.color,this.name);
}

class Player {
  final String id;
  final String name;
  const Player(this.id, this.name,);
}